# Copy this file to terraform.tfvars and adjust if you like.
# Both values have sensible defaults, so terraform.tfvars is optional.

prefix   = "swat"
location = "eastus"     # allowed: eastus, eastus2, canadacentral
